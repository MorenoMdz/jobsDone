{
  "name": "dailydone",
  "version": "1.0.0",
  "description": "Fake Rest API for the dailydone project",
  "main": "index.js",
  "dependencies": {
    "concurrently": "^4.1.0",
    "json-server": "^0.14.2"
  },
  "devDependencies": {},
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "json:server": "json-server --watch db.json --port 3004",
    "client": "yarn --cwd ../client/jobsdone/ run start",
    "start": "concurrently \"npm run json:server\" \"npm run client\""
  },
  "author": "",
  "license": "ISC"
}
